・なぜpropsに interface を作るのが大事か（自分の言葉で）
    道具に引き渡しという不安定な部分なので型宣言をすることで、エラーを修正しやすい

・onToggle: (index: number) => void という書き方は何を意味するか
    onToggle=App.tsxから借りてきた道具（関数）」引数indexは数字と型宣言。なにも返さない。

・既存JSにTSを後付けする経験をしてどう感じたか
    いままでのコードがどんな値も受け取るものだったので不安定だなと感じた。